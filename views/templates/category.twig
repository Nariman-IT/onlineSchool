
    <main>
    
        {% for item in resultSubCategories %}
            <div class="main" id={{ item.id }}><img class="categoryPictures" src="../public/image/{{ item.image }}.jpg" alt="{{ item.name }}"></div>
            <div class="description" id={{ item.id }}><h2>{{ item.name }}</h2> {{ item.description }}</div>
        {% endfor %}

    </main>
